# Python 기반 실행 환경
FROM python:3.10-slim

# 작업 디렉토리 설정
WORKDIR /app

# 파이썬 의존성 파일 복사 및 설치
# requirements.txt 파일이 backend 폴더 안에 있다고 가정
COPY backend/requirements.txt requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# 애플리케이션 코드 복사
COPY backend/ .

# 포트 노출 (예: Flask/Django 기본 포트)
EXPOSE 5000

# 애플리케이션 실행 명령어 (실제 사용하는 백엔드 프레임워크에 맞게 수정 필요)
# 예시: Flask를 gunicorn으로 실행
CMD ["gunicorn", "--bind", "0.0.0.0:5000", "app:app"]
